meta {
  name: Get state of a DID document
  type: http
  seq: 4
}

post {
  url: {{IDENTITY_PATH}}/v1beta/participants/{{PARTICIPANT_CONTEXT_ID}}/dids/state
  body: json
  auth: apikey
}

auth:apikey {
  key: X-Api-Key
  value: {{API_KEY}}
  placement: header
}

body:json {
  {
    "did": "{{DID_PLAIN}}"
  }
}
